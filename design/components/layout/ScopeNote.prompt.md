The provenance footnote at the bottom of the sidebar.

```jsx
<ScopeNote>curated demonstration fixture</ScopeNote>
```

Renders `incident.dataScope` verbatim. Keep it visible on desktop — it is the honesty line that stops the console from implying ecosystem-scale coverage. It is hidden below 820px only because the source hides it there.

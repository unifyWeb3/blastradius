Square status glyph that leads an application row or an inline verdict.

```jsx
<StatusChip status="exposed" />
<StatusChip status="unresolved" size={22} />
```

Same status vocabulary as `StatusPill`. Pair it with the row's text label — the chip alone is not a sufficient status carrier.

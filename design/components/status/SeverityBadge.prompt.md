Severity badge for the advisory. The curated incident is always `critical`.

```jsx
<SeverityBadge severity="critical" />
```

Filled red, never outlined — severity is the loudest thing in the sidebar and must not be mistaken for a filter chip.

Uppercase 10px eyebrow — the console labels everything before it shows a value.

```jsx
<Kicker>Exposed applications</Kicker>
<Kicker tone="danger">GHSA-PJWM-RVH2-C87W</Kicker>
```

Use above metrics, panel headings, sidebar fields and evidence sections. `tone="danger"` is only for advisory identity; `tone="primary"` lifts it to secondary text when a kicker sits on a dark panel header.
